// Command pr-mon monitors GitHub pull requests: the dashboard, and the backend
// that polls, notifies and merges. (Port in progress; see
// docs/superpowers/plans/2026-09-17-go-rewrite.md.)
package main

import "fmt"

func main() {
	fmt.Println("pr-mon: the Go port is still being built; use the Python CLI for now")
}
